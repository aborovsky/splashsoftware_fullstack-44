import { WsexceptionFilterFilter } from './wsexception-filter.filter';

describe('WsexceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new WsexceptionFilterFilter()).toBeDefined();
  });
});
